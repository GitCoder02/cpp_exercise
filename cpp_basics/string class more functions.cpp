// string class more functions.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include<string>
using namespace std;
int main()
{
    string s = "Hello World";
    //string s2 = "hello";
    //char s2[10];
    //s.replace(3, 4, "are");
    //s.erase();
    //s.push_back('G');
    //s.pop_back();
    //s.swap(s2);
    //s.copy(s2, s.length());
    //s.copy(s2, 3);
    //s2[3] = '\0';
    //cout << s.rfind('l')<<endl; --> 3
    // cout << s.find('l')<<endl; --> 2
    //cout << s << endl;
    //cout << s2 << endl;
    //cout << s.find_first_of('l',4) << endl;   starts searching l from index 4.
    //cout << s.find_last_of('l') << endl;
    //cout << s.find_first_of("le") << endl;  will search for either 'l' or 'e' whichever appears first.
    //cout << s.substr(3,4); will give the substring from index 3 onwards and, 4 denotes that give only till the next 4 index.
    //cout << s.compare(s2) << endl;

    return 0;

}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
